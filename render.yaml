services:
  # ── バックエンド: FastAPI Web Service ─────────────────────────────
  - type: web
    name: persona-api
    runtime: python
    runtimeVersion: "3.11.8"
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: GEMINI_API_KEY
        sync: false          # Renderダッシュボードで手動設定（シークレット）
      - key: GEMINI_MODEL
        value: gemini-1.5-flash
      - key: BULK_CONCURRENCY
        value: "3"
      - key: FRONTEND_ORIGIN
        value: https://persona-frontend-5kws.onrender.com
    healthCheckPath: /api/usage
    plan: free

  # ── フロントエンド: Static Site ───────────────────────────────────
  - type: web
    name: persona-frontend
    runtime: static
    rootDir: frontend
    staticPublishPath: .
    buildCommand: ""
